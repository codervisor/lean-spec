{
  "name": "Minimal",
  "description": "Just the essentials - folder structure only",
  "config": {
    "template": "minimal",
    "specsDir": "specs",
    "structure": {
      "pattern": "{date}/{seq}-{name}/",
      "dateFormat": "YYYYMMDD",
      "sequenceDigits": 3,
      "defaultFile": "README.md"
    }
  }
}
